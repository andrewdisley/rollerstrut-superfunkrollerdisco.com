{#
  Single 11ty component macro for all components
  Credit: https://www.trysmudford.com/blog/encapsulated-11ty-components/
#}
{%- macro component(name, params) -%}
  {% include "components/" + name + "/" + name + ".njk" ignore missing %}
{%- endmacro -%}
